apiVersion: v1
kind: Service
metadata:
  name: devboard-fe-service
  namespace: devboard
spec:
  selector:
    app: devboard-fe
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 5173
      nodePort: 30080
